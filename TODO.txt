Only generate averages report on request, not on every race.

Make it clear which season is being edited:
    Add date of season to window or titlebar?

Change to use '-' character instead of ':' when entering times/dates.

Bug: Doesn't show members in list correctly "Joe Blogs, ,"

Drop getters for read-only fields in model files.

Update unit tests for averages.

    Including regression extended test.

Revisit which bits of functionality are needed for club vs fed.

Migrate tests to JUnit 4.

HTML escape report outputs.

