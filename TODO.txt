Converting between fed and club modes leaves the member data messy.

Drop getters for read-only fields in model files.

Update unit tests for averages.

    Including regression extended test.

Revisit which bits of functionality are needed for club vs fed.

Migrate tests to JUnit 4.

HTML escape report outputs.

